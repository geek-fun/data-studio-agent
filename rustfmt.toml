# Rustfmt configuration for data-studio-agent workspace
# Stable-only options — CI validates with stable `cargo fmt`.

edition = "2021"
max_width = 100
tab_spaces = 4
hard_tabs = false
use_small_heuristics = "Max"
match_block_trailing_comma = true
