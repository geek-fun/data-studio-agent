# Clippy configuration for data-studio-agent workspace

# Allow some reasonable patterns
allow-dbg-in-tests = true

# Cognitive complexity threshold
cognitive-complexity-threshold = 30

# Too many arguments is expected for Tauri command wrappers
too-many-arguments-threshold = 10

# Allow the async_trait pattern for trait methods returning impl Future
# (required by the SessionStore async trait pattern)
